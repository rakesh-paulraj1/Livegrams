{
  "$schema": "https://turborepo.com/schema.json",
  "ui": "tui",
  "globalEnv": [
    "GOOGLE_CLIENT_SECRET",
    "GOOGLE_CLIENT_ID",
    "NEXTAUTH_SECRET"
  ],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [".next/**", "!.next/cache/**"],
      "env": ["GOOGLE_CLIENT_SECRET", "GOOGLE_CLIENT_ID", "NEXTAUTH_SECRET"]
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "check-types": {
      "dependsOn": ["^check-types"]
    },
    "dev": {
      "cache": false,
      "persistent": true,
      "env": ["GOOGLE_CLIENT_SECRET", "GOOGLE_CLIENT_ID", "NEXTAUTH_SECRET"]
    }
  }
}
